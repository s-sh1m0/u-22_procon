# GitHub OAuth
GITHUB_CLIENT_ID=your_client_id
GITHUB_CLIENT_SECRET=your_client_secret
GITHUB_CALLBACK_URL=http://localhost/auth/github/callback

# Backend
PORT=8080
SESSION_SECRET=change_this_to_random_string
DB_PATH=/app/data/reviewarena.db

# 解析ジョブ（任意・省略時はデフォルト値）
# JOB_WORKERS: 同時に走らせる解析ワーカー数（省略時 = min(CPU数, 4)）。
#   1ジョブは型チェック＋SSA/CHA で CPU/メモリを多く使うため、GOMEMLIMIT/mem_limit と合わせて調整する。
# JOB_TIMEOUT_SECONDS: 1ジョブの解析時間上限（省略時 120）。巨大リポジトリの暴走対策。
# JOB_QUEUE_BUFFER: ジョブキューのバッファ長（省略時 32）。
# JOB_WORKERS=4
# JOB_TIMEOUT_SECONDS=120
# JOB_QUEUE_BUFFER=32

# --- デプロイ (Vercel) ---
# 本番は Vercel Dockerfile デプロイ (same-origin) のため、以下は通常不要。
# フロント・バックを別ドメインに分離する場合のみ設定する。
# FRONTEND_URL=
# VITE_API_BASE_URL=

# STATIC_DIR: フロントエンドのビルド成果物ディレクトリ。
#   本番 Dockerfile 内で自動設定される (=/app/frontend)。
#   ローカル開発 (nginx プロキシ) では空のままにする。
# STATIC_DIR=
